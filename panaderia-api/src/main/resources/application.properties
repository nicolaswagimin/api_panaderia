## Server
server.port=${PORT:8080}

# PostgreSQL
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://dpg-d8bs2igjo6nc73eabj40-a.ohio-postgres.render.com:5432/panaderia_oof6?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:panaderia_user}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:s4b2pXoSK67FjNHbTcKMHjBtefzBx9iW}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=false

# JWT
jwt.secret=${JWT_SECRET:clave-super-secreta-panaderia-2024-muy-larga-para-seguridad}
jwt.expiration=${JWT_EXPIRATION:86400000}

# App
spring.application.name=panaderia-api

# Sirve index.html para rutas no encontradas (Angular SPA)
spring.web.resources.static-locations=classpath:/static/
spring.mvc.throw-exception-if-no-handler-found=false
spring.web.resources.add-mappings=true
